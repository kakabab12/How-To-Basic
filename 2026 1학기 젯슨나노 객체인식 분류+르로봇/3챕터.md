# 3챕터 — 폴더 구조와 lerobot의 정체

> **젯슨 나노 객체인식 분류 + 르로봇** · 3/15챕터 · [📚 목차](README.md)

---

```
2026 1학기 젯슨나노 객체인식 분류+르로봇/
├── 코드설명.md                    ← 본인이 쓴 요약 (읽을 것)
└── project/
    ├── jetson_USB2.py             ★ 최종 완성본 (7~14단계)
    ├── jetson_USB.py              최종본 형제
    ├── jetson_csi_final.py        Plan A
    ├── jetson_main1(9).py         Plan A
    ├── jetson_main1.py            0세대
    ├── turnonnx.py                TensorRT 변환 (5단계)
    ├── cam_check.py / imx.py / testgpu.py / test_dofBot.py   진단 도구
    ├── box.pt / box.onnx / box.engine          박스 모델 3형태 (4단계)
    ├── number.pt / number.onnx / number.engine 숫자 모델 3형태
    ├── index.html                 대시보드 (14단계)
    ├── templates/index.html       Flask 관습 잔재
    ├── onnxruntime_gpu-1.17.0-cp310-cp310-linux_aarch64.whl   젯슨 전용 휠
    ├── 로봇팔.txt                  실행 순서 메모 (15단계)
    ├── *.log                      실제 운영 로그
    ├── RIP/                       폐기된 시도들 + app_ck.py/camera.py (라즈베리파이 잔재)
    └── lerobot/                   ← 376개. 본인 코드 아님 (15단계)
```

#### `lerobot/`은 남의 코드다

**Hugging Face의 `lerobot` 라이브러리를 통째로 넣은 것**이다(Apache-2.0).
커밋 메시지 `Fix: Add lerobot as a normal folder`가 그 흔적으로, 원래 git submodule이던 걸
일반 폴더로 바꿔 넣었다.

**본인이 작성한 파일은 `lerobot/act_floor1_test.py` 하나**다 (15단계에서 다룬다).

> **파일 수에 속지 말 것.** 416개 중 376개가 라이브러리다. 실제로 읽을 코드는 40개고,
> 그중에서도 핵심은 `jetson_USB2.py` 하나다.

---

◀ [2챕터 — Plan A → Plan B — 파일 계보 읽는 법](2챕터.md) · [📚 목차](README.md) · [4챕터 — 모델이 3가지 포맷으로 있는 이유](4챕터.md) ▶
