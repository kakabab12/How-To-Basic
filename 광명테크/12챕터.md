# 12챕터 — 8층 이벤트 송출 — 키오스크 접점

> **광명테크 제스처 인식** · 12/15챕터 · [📚 목차](README.md)

---

`src/pipeline/event_sender.py`

```python
class ConsoleEventSender:
    """예시 구현 1 — 이벤트를 로그로만 기록한다."""
    def send(self, gesture_event):
        logger.info("event_output(console): %s (conf=%.2f)", ...)

class UdpEventSender:
    """예시 구현 2 — 이벤트를 JSON으로 UDP 전송한다."""
    def send(self, gesture_event):
        payload = {"class_name": ..., "conf": round(..., 4), "ts_sec": ...}
        if gesture_event.hand_side is not None:
            payload["hand_side"] = gesture_event.hand_side
        self._sock.sendto(json.dumps(payload, ensure_ascii=False).encode("utf-8"), self._addr)

def create_event_sender(config):
    mode = config["event_output"]["mode"]
    if mode == "udp":    return UdpEventSender(config)
    if mode == "console": return ConsoleEventSender()
    raise ValueError(f"지원하지 않는 event_output.mode: {mode}")
```

파일 첫 주석이 **의도를 명시**한다:

> 새 수신 규격(소켓/시리얼 등)이 정해지면 이 파일에 전용 Sender를 추가하고
> config의 event_output.mode로 선택한다.

**팀 협업의 미결정 사항을 코드 구조로 흡수한 것**이다. 키오스크 팀이 어떤 방식으로 받을지
아직 안 정해졌으니, **확장 지점만 만들어 두고** 두 가지 예시를 넣어 뒀다.
`create_event_sender`는 팩토리 패턴이고, 모르는 모드는 조용히 넘어가지 않고 **에러를 낸다**.

---

◀ [11챕터 — 7층 판정 FSM — 흔들림을 이벤트로 바꾸기](11챕터.md) · [📚 목차](README.md) · [13챕터 — 조립 — 파이프라인과 실행](13챕터.md) ▶
